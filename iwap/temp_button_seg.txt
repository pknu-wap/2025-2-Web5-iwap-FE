<button
          onClick={toggleDrawing}
          className={`px-3 py-1 rounded font-medium ${isDrawing ? "드로잉 중" : "대기"}`}
          title="UI 踰꾪듉?쇰줈 ?쒕줈???쒖옉/?뺤?"
        >
          {isDrawing ? "?쒕줈??以? : "?湲?}
        </button>\n        <label class="flex items-center gap-2 text-sm">\n          제스처\n          <input type="checkbox" checked={gestureEnabled} onChange={(e) => setGestureEnable
